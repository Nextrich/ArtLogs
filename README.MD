# ArtLogs 📝

![Python Version](https://img.shields.io/badge/python-3.7+-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

**ArtLogs** — это мощная и красивая библиотека для структурированного логирования с поддержкой профилей пользователей, цветного вывода в консоль и гибкой настройки режимов логирования.

## ✨ Особенности

- 🎨 **Цветное логирование** с использованием Colorama
- 👤 **Система профилей** с авторизацией
- 📁 **Автоматическое сохранение** логов в файлы
- 🎛️ **Гибкие режимы** логирования (info, warning, error, success)
- 🔐 **Безопасное хранение** данных профилей
- 🚀 **Простой и интуитивный** API

# 📦 Установка

### Установка через pip:
```bash
pip install artlogs
```

🚀 Быстрый старт
Создание профиля и базовое логирование:
```python
from artlogs import ArtLogs

# Создаем менеджер профилей
manager = ArtLogs()

# Создаем новый профиль
profile = manager.logUser("MyProfile", "secure_password")

# Или авторизуем существующий
# profile = manager.logUser("MyProfile", "secure_password", "i")

# Логирование
profile.log("Это информационное сообщение", "i")
profile.log("Успешная операция!", "a")
profile.log("Предупреждение!", "w")
profile.log("Ошибка!", "e")
```
# Работа напрямую с профилем:
```python
from artlogs import ArtLogProfile

# Создание профиля
profile = ArtLogProfile("User1", "password123")

# Или авторизация существующего
# profile = ArtLogProfile("User1", "password123")
# Профиль автоматически попытается авторизоваться, если переданы имя и пароль

# Логирование
profile.log("Привет, мир!", "a")
```
# 📖 Документация
## Класс ```ArtLogs``` (Менеджер профилей)
Менеджер для работы с несколькими профилями.

**Методы**:
1. ```logUser(name="user", password="", flag="c")``` - создает новый профиль

2. ```logUser(name="user", password="", flag="i")``` - авторизует существующий профиль
## Класс ArtLogProfile (Профиль)
Основной класс для работы с логированием.

**Инициализация:**
```python
logs = artlogs.ArtLogs()
user1 = logs.logUser(name="user1", password="1user")
```
Если переданы ```name``` и ```password```, профиль автоматически создается.

**Основные методы:**

**Работа с профилем:**

```logUser(name="user", password="0000", flag="c")``` - создать/авторизовать профиль

```delUser()``` - удалить профиль

```clear_logs()``` - очистить файл логов

**Логирование:**

```log(text, flag="i")``` - публичный метод для логирования

```set_mod(flag="all")``` - изменить режим логирования

**Флаги логирования:**

```"i"``` - информационное сообщение (синий)

```"a"``` - успешная операция (зеленый)

```"w"``` - предупреждение (желтый)

```"e"``` - ошибка (красный)

**Режимы логирования:**

```"all"``` - все типы логов (по умолчанию)

```"i"``` - только информационные

```"a"``` - только успешные операции

```"w"``` - только предупреждения

```"e"``` - только ошибки

# Примеры использования

## Пример 1: Полный цикл работы

```python
from artlogs import ArtLogs

manager = ArtLogs()

# Создание профиля
profile = manager.logUser("DevUser", "dev123")

# Изменение режима логирования
profile.set_mod("e")  # Теперь будут выводиться только ошибки

# Логирование
profile.log("Это сообщение не появится", "i")  # Не выведется
profile.log("Ошибка базы данных", "e")  # Выведется

# Возврат к выводу всех логов
profile.set_mod("all")

# Очистка логов
profile.clear_logs() 
```
## Пример 2: Работа с несколькими профилями
```python
from artlogs import ArtLogs

manager = ArtLogs()

# Создаем несколько профилей
admin = manager.logUser("Admin", "admin_pass")
user1 = manager.logUser("User1", "pass123")
user2 = manager.logUser("User2", "pass456")

# Логируем от разных пользователей
admin.log("Система запущена", "a")
user1.log("Пользователь вошел в систему", "i")
user2.log("Неверный пароль", "e")

# Удаление профиля
user2.delUser()
```

## Пример 3: Продвинутые настройки
```python
from artlogs import ArtLogProfile
```

## Профиль с кастомными настройками
```
profile = ArtLogProfile("TestUser", "test123")
```

## Работа с режимами
```
profile.set_mod("w")  # Только предупреждения
```

## Проверка доступа
```
if profile.access:
    print(f"Пользователь {profile.username} авторизован")
```
    
## Получение имени файла логов
```
print(f"Логи сохраняются в: {profile.log_filename}")
```
# 📁 Структура файла логов
Каждый профиль сохраняет логи в файл с именем ```[username].log```:

```text
===ArtLogs===
name=username,
password=password,
===ArtLogs===
✅Успешное сообщение✅
⚠️Предупреждение⚠️
⛔Ошибка⛔
```

#  ⚠️ Важные замечания
**Безопасность**: Пароли хранятся в открытом виде в файлах логов. Не используйте реальные пароли!

**Цветной вывод**: Для работы цветного вывода требуется установленный colorama

**Кодировка**: Все файлы сохраняются в UTF-8

**Имена файлов**: Имена файлов логов соответствуют именам пользователей

# 🐛 Отладка
Если возникают проблемы с цветным выводом:

```python
# Проверка установки colorama
import colorama
colorama.init()  # Инициализация для Windows
```

# 🤝 Вклад в разработку
1. Форкните репозиторий

2. Создайте ветку для вашей функции (git checkout -b feature/AmazingFeature)

3. Зафиксируйте изменения (git commit -m 'Add AmazingFeature')

4. Запушьте ветку (git push origin feature/AmazingFeature)

5. Откройте Pull Request

# 📄 Лицензия
Распространяется под лицензией MIT. Смотрите файл LICENSE для подробностей.

# ✍️ Автор
Na9ash1 (ArtProgsTeam)

###  ⭐ Если этот проект был полезен, поставьте звезду на GitHub!

